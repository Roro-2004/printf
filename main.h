#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>

int _strlen(char *s);
int _printf(const char *format, ...);
int _putchar2(char r);
int put(char *);

#endif
