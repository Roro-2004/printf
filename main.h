#ifndef MAIN_H
#define MAIN_H
#define int_minn -2147483648

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <limits.h>

int _strlen(char *s);
int _printf(const char *format, ...);
int _putchar2(char r);
int put(char *);
int putnum(int n);
int count(int n);
void int_min(int n);
int unsign(int n);

#endif
