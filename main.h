#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

int _strlen(char *s);
int _printf(const char *format, ...);
int _putchar2(char r);
int put(char *);
int putnum(int n);
int count(int n);

#endif
